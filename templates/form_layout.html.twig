{% use "tailwind_2_layout.html.twig" %}

{%- block form_help -%}
    {%- set help_attr = help_attr|merge({ class: help_attr.class|default(help_class|default('mt-2 text-sm text-gray-2')) }) -%}
    {{- parent() -}}
{%- endblock form_help -%}